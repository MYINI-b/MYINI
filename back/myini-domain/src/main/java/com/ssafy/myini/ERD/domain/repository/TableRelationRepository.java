package com.ssafy.myini.ERD.domain.repository;

import org.springframework.data.jpa.repository.JpaRepository;

public interface TableRelationRepository extends JpaRepository<TableRelationRepository, Long> {
}
